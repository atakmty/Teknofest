process {
    executor = 'local'
    errorStrategy = 'retry'
    maxRetries = 1
}

conda {
    enabled = true
    useMamba = false
}

// Windows WSL / Linux uyumluluğu için
profiles {
    standard {
        process.executor = 'local'
    }
}
